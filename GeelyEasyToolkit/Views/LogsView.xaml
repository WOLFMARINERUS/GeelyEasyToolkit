<UserControl x:Class="GeelyEasyToolkit.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- Заголовок -->

        <TextBlock Grid.Row="0"
                   Text="Логи"
                   FontSize="30"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,10"/>


        <TextBlock Grid.Row="1"
                   Text="Журнал действий программы и диагностики."
                   Foreground="#C8CDD6"
                   FontSize="14"
                   Margin="0,0,0,15"/>


        <!-- Журнал -->

        <Border Grid.Row="2"
                Background="#252932"
                BorderBrush="#596273"
                BorderThickness="1"
                CornerRadius="8">

            <TextBox x:Name="LogTextBox"
                     Background="#252932"
                     Foreground="#D8DEE9"
                     BorderThickness="0"
                     Padding="15"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="13"/>

        </Border>


        <!-- Нижняя панель -->

        <Grid Grid.Row="3"
              Margin="0,15,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>


            <TextBlock x:Name="LogFileText"
                       VerticalAlignment="Center"
                       Foreground="#9EA5B3"
                       Text=""/>


            <Button Grid.Column="1"
                    Content="Обновить"
                    Width="110"
                    Height="38"
                    Margin="10,0,0,0"
                    Click="RefreshButton_Click"/>


            <Button Grid.Column="2"
                    Content="Копировать"
                    Width="110"
                    Height="38"
                    Margin="10,0,0,0"
                    Click="CopyButton_Click"/>


            <Button Grid.Column="3"
                    Content="Очистить"
                    Width="100"
                    Height="38"
                    Margin="10,0,0,0"
                    Click="ClearButton_Click"/>

        </Grid>

    </Grid>

</UserControl>